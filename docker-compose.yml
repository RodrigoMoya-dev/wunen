version: "3.9"

services:

  db:
    image: postgres:16-alpine
    container_name: wunen_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-wunen}
      POSTGRES_USER: ${POSTGRES_USER:-wunen}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-wunen_secret}
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-wunen}"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: wunen_backend
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-wunen}:${POSTGRES_PASSWORD:-wunen_secret}@db:5432/${POSTGRES_DB:-wunen}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
      ENVIRONMENT: ${ENVIRONMENT:-development}
    volumes:
      - ./backend:/app
      - playwright_cookies:/app/cookies
    ports:
      - "8000:8000"
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  scraper:
    build:
      context: ./scraper
      dockerfile: Dockerfile
    container_name: wunen_scraper
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-wunen}:${POSTGRES_PASSWORD:-wunen_secret}@db:5432/${POSTGRES_DB:-wunen}
      BACKEND_URL: http://backend:8000
    volumes:
      - ./scraper:/app
      - playwright_cookies:/app/cookies
    # El scraper corre bajo demanda invocado por el backend/scheduler

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: wunen_frontend
    restart: unless-stopped
    depends_on:
      - backend
    environment:
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:8000}
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    ports:
      - "3000:3000"
    # Modo producción: evita la compilación on-demand del modo dev (menú lento /
    # webpack hot-update). Reconstruye desde el código montado en cada arranque.
    command: sh -c "npm run build && npm start"

volumes:
  db_data:
    driver: local
  playwright_cookies:
    driver: local
